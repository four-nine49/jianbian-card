# 渐变带 · 双桌工作台 会话交接文档（2026-09 会话）

> **给接手的 AI**：这份文档记录本次会话对「双桌工作台」（`src/gradband/assets/statusbar-desk.html`）的全部改动、
> 当前状态、已否决方案与待办。**先读这份，再读 `渐变带交接文档.md`（架构总纲）与 `状态栏HTML适配指南.md`（数据契约）。**
> 三份文档配合 `自由构筑-计算引擎-完整交付.md`（给外部 AI 的自由构筑引擎交付包）即可完整接手。

---

## 0. 当前状态一句话

- 仓库 `four-nine49/jianbian-card`，**git HEAD = d04cc93 = v1.5.0（已提交已推送）**
- **工作区有一大堆未提交改动**：`build.mjs`、`src/gradband/assets/statusbar-desk.html`、`dist/`、
  `渐变带交接文档.md`、`状态栏HTML适配指南.md`、`酒馆助手脚本-开局.json`（loader 重生成过）——全部是本次会话的
  工作台演进，**尚未提交、版本号未升（仍 1.5.0）**
- `git stash@{0}`：`src/gradband/core/settings.ts` + `src/gradband/feel-tables.ts` 的"感情AI每表提示词清理"
  被用户摘出暂存（**发布时不含**，用户说不要；保留等用户决定恢复/丢弃）

---

## 1. 工作台现状（statusbar-desk.html 已实现的功能清单）

**三桌结构**（顶栏 `[1][2][3]` 切换，视口 860px 高、宽不锁随消息）：

| 桌 | 内容 |
|---|---|
| 1 回路配置桌 | 监视器栏（身体/能量精神/脑神经负荷/战斗锁）+ 槽位托盘 + 回路卡（拖拽换槽/标记已用） |
| 2 角色桌 | 施法者横幅 + 5 列 18 张亲和卡（主/次/非亲和发光） |
| 3 自由施法桌 | 锻造台：五族电闸 + 参数调制机架（动态控件）+ 核算报价仪 + 意图送审（申报入自由库/临时强冲） |

**关键机制（本轮会话全部落地，别回退）**：

1. **广告牌模式（Billboard）**：视口 `perspective:1600px`；桌面 `rotateX(var(--tilt))`（`--tilt:12deg` 在 `:root`）；
   卡片/面板反旋 `rotateX(calc(-1 * var(--tilt)))` 正视（回路卡含拖拽态、亲和卡含 hover、锻造台三块面板）。
   监视器/槽位托盘/横幅平铺在桌面（保证卡片吸附坐标与桌面同一变换系）。
2. **固定缩放档位（zoom 重排，全档清晰）**：`ZOOM_LEVELS = [0.5,0.8,1.0,1.3,1.6,2.0]`，滚轮步进，初始 1.0；
   缩放走 `element.style.zoom = Z`（重排，非 transform scale）；平移量按 zoom 折算（`pan += delta/Z`）；
   `scale1/2/3` 已全部删除；槽位坐标/卡片拖拽除法用 `zoom1`。**改 upd/滚轮/平移三处 JS 时别改回 scale。**
3. **槽位容量精神化**：`FIXED_CAP = clamp(⌊精神上限/10⌋, 2, 8)`；`FREE_CAP = clamp(⌊精神上限/30⌋, 1, 4)`；
   在 `loadFromSave` 计算，`buildSlots`/截断读取/写回索引全走容量；`slotCapText()` 显示公式。
4. **锻造台数据全接真**（原型是 mock，已换）：报价走引擎 `E.quote` + 真实 ctx（`forgeCtx()`：
   aff/scene/char/tuned）；`申报入自由库` = writeGame 推 `回路库`（`fr-XX`、来源='面板送审'、uses=0、
   参数明细/基线账单现算、微调预算 null）；`临时强冲` = writeGame 推 `待扣单`（makePending + 锚点/famKey）。
5. **灯箱桌面 + 精神辉光**：`--light-*` 变量（:root）；`LIGHT_STOPS` 五档色标（满额天青→薄荷→琥珀→橙红→血崩红）；
   `setMentalEnergy(精神点.当前)` 按 当前/上限 比例插值改写 `--light-r/g/b`；`renderMonitors` 驱动 + 10 秒轮询
   （`setInterval` 只刷监视器+灯光，不重建卡片）。顶栏「精神力能级」+ 桌1「脑神经负荷」表盘。
6. **三桌内容居中**：桌1 `.desk1-center`（50%/50% + translate(-50%,-50%)）包监视器+槽位托盘；桌2 `justify-content:center`；
   桌3 `.forge-layout` 居中。**初始视角 pan 0,0（居中）。**
7. **主亲和卡牌全彩**：图案/左上角族名/左侧竖线全上色（`status-primary .aff-svg-wrap/.aff-head/.aff-title-box`），
   叠加霓虹外壳动画。
8. **18 支完整 SVG + lore**：从 `C:\Users\HP\Downloads\gradient_zone_full_movable_desks.html` 全量移植
   （光 8 向、核力场三叶原子图腾等多行模板字符串）；**id 没动**（`aff_pour_光` 这套，亲和匹配依赖）。
9. **侧栏**：固定视口最右（`right:0; top:48px; height:calc(100vh-48px); width:380px`），加载自动打开；
   竖条开关 + 点卡片自动切档案 tab。
10. **防糊**：`-webkit-font-smoothing:antialiased` + `text-rendering:optimizeLegibility`（#gdesk）；无 3D 透视的
    rotateX 已证明无透视效果（正交投影=scaleY(cosθ)），桌面倾斜必须配 `perspective` 才是真 3D。

**构建产物**：`dist/状态栏面板-双桌.html` = 引擎内联（`/*@ENGINE@*/`）的直接 HTML（**无 iframe**），
正则注入：查找 `<StatusPlaceHolderImpl/>`，全文替换。旧 `dist/状态栏面板.html` 仍是流式旧版（并存）。

---

## 2. 已否决方案（重要！别让下个 AI 重走弯路）

| 方案 | 否决原因 |
|---|---|
| **iframe srcdoc 包裹**（墨月写卡工坊模式：整页塞 iframe 注入） | 用户明确否决 |
| **内联布局小修**（`#gradientDesk` 容器化、fixed→absolute、100vw→100%） | 实测"没有用"，视觉仍崩 |
| **伪 3D 三棱柱状态栏**（skewX 顶面 + clip-path 三角端面 + 渐变分色） | 用户"效果太烂了"，已回档（本次会话最后一步） |
| transform:scale 非 1.0 缩放 | 必糊（纹理重采样），缩放只能走 `zoom` 重排 |

**必守规则**（改 statusbar-desk.html 时）：
- `body`/`html` 零 CSS（全归 `#gdesk`）；未开局兜底必须 `document.getElementById('gdesk')`，禁 `document.body`
- 引擎只算数，AI/玩家不得自行算术；`E.quote` 必须传完整 ctx
- 槽位写回、送审入库、待扣单必须走 `writeGame`
- 数据出口过宏替换；参数明细用 `readableParams`

---

## 3. 原型参考文件（都在用户 Downloads）

- `gradient_zone_full_movable_desks.html` —— 18 支 SVG/lore 母版（已移植）
- `gradient_zone_tri_workbench.html` —— 三桌 + 锻造台设计母版（已移植，mock 数据已换真）
- `gradient_zone_lightbox_mental_glow.html` —— 灯箱桌面 + 精神辉光母版（已移植）

---

## 4. 验证命令（每次改完必跑）

```bash
cd src/渐变带-面板版
node build.mjs                                # 产 dist（含 状态栏面板-双桌.html，引擎内联）
node ../开局框架-扩展/node_modules/typescript/bin/tsc --noEmit -p tsconfig.json
node smoke.mjs                                # 冒烟
node package-loader.mjs                       # 改了 manifest 版本才跑
```

引擎回归（改了引擎相关才跑）：`src/渐变带-自由回路/test/run.mjs`（46 用例）。

---

## 5. 待办 / 注意

- **提交/发布未做**：全部工作区改动未提交，版本仍 1.5.0。用户确认效果后：`git add -A && commit && push origin main`
  + `git tag v1.6.0 && push`（tag 名先 `git tag --list` 查占用）；或升三处版本号（manifest/package.json/version.ts）
  走 1.5.1。**注意 stash 里的 settings.ts/feel-tables.ts 用户明确不要，别 add 进去**（`git stash drop` 可弃）。
- **卡片吸附 ~2% 纵向偏差**：透视桌面近大远小的固有现象（面板正视 vs 桌面平铺），视觉可接受，别"修"。
- **zoom 属性**需现代浏览器（Chrome/Edge/Safari 全支持，Firefox ≥126）。
- **槽位容量是工作台行为**：存档数组仍 10/3，超出精神容量的已装卡不显示——规则如此。
- 用户换对话框时的交接包 = 本文件 + `渐变带交接文档.md` + `状态栏HTML适配指南.md` + `自由构筑-计算引擎-完整交付.md`。

---

*本会话结束于 2026-09；最后动作：prism 伪 3D 已回档，billboard/zoom 档位/灯箱/锻造台等已确认状态全部保留。*
