# 交接文档 — v1.8.6（送审状态 + 精神门控修复；含 v1.8.2→v1.8.6 全量）

> **给接手的 AI**：**先读根目录 `AGENTS.md`**（架构总纲、目录地图、数据通道、发布规矩）；
> 技术细节（引擎公式 / 状态栏 HTML 契约 / 世界书参考 / 提示词契约）按需查 `docs/具体介绍.md`。
> 本文是 **2026-09-13 这一阶段的状态快照**，覆盖上一份交接文档（v1.8.1）之后到 **v1.8.6** 的全部改动、
> 已发布版本、验证状态与遗留。`归档/` 里的旧文档仅为历史快照，**不代表现状**。
> 本文件按惯例：下一轮会话结束后移入 `归档/` 并加「已归档」横幅。

---

## 0. 当前状态一句话

- 仓库 `four-nine49/jianbian-card`，工作目录 `src/渐变带-面板版/`；**HEAD = `c0095aa` = v1.8.6**，
  最新 tag = **v1.8.6**（轻量标签），`origin/main` 与远端 tag 均已同步，**无未发布提交**。
- **引擎号 `2.3.0`**（`src/gradband/engine/circuit-engine.js` 的 `VERSION`）；扩展版本 **1.8.6**（三处同步）。
- 工作树**只剩一个未提交改动**：`docs/世界书条目-渐变带状态注入.txt`（**用户手改、刻意未纳入提交**，
  见 §5.1）——其余全部干净。
- 验证链全绿：`node build.mjs` ✅ → `tsc --noEmit` ✅ 0 错误（1 条无害的 CommonJS-in-ESM 警告）→
  `node smoke.mjs` ✅（singleton `1.8.6` ready）→ `node package-loader.mjs` ✅ 23 项自检 →
  两份 regex 包与对应 dist HTML **逐字节一致**。
- 本环境构建无障碍（`node_modules` 已装，esbuild/tsc 本地可跑）。

---

## 1. 本阶段主题（v1.8.2 → v1.8.6）

| 版本 | commit / tag | 一句话 |
|---|---|---|
| **v1.8.2** | `fdd090d` / v1.8.2 | 修「情感分析」**幽灵勾选框**——渐变带设置 `开关` 加载改**白名单**，旧档残留键不再渲染 |
| **v1.8.3** | `b9365f1` / v1.8.3 | 回路新增字段 **`物理相态与表征`**（正文AI 叙事唯一形态锚点）+ 法术AI 提示词换新 4 段契约 |
| **v1.8.4** | `8152871` / v1.8.4 | **开局送的固定回路补齐 `物理相态与表征`**（v1.8.3 只覆盖了送审路径，开局全缺） |
| **v1.8.5** | `6045a29` / v1.8.5 | **修开局固定回路精神 ×4 门控 bug**（亲和 ctx 键名错位）+ **固化回路精神 ÷5**（新折扣） |
| **v1.8.6** | `c0095aa` / v1.8.6 | 状态栏送审加**「送审中」可见状态**（送审槽 + 自定义回路两个入口） |

（另有 `5ca66fd`：世界书条目 15 适配新回路结构，**纯文档、不发版**；`103ecae`/`1b5ff38`/`86618eb`：文档与 .gitignore 杂项。）

**用户需重导正则的历史**：v1.8.3、v1.8.4、v1.8.5、v1.8.6 均改了 HTML 资产 →
v1.8.5 **首次两包齐动**（开局 + 状态栏都要重导）；v1.8.6 只动状态栏。**当前两份 regex 包都是最新的。**

---

## 2. 改动明细（按版本）

### 2.1 v1.8.2 —「情感分析」幽灵勾选框

- 渐变带设置页的开关是**按已存数据的键动态渲染**的（`ui/pages/gradband.ts` 用 `Object.keys(s.开关)` 渲染，
  文案就是键名本身）。`gradband/core/settings.ts` 加载时原本 `{ ...def.开关, ...(raw.开关||{}) }` **没有白名单**，
  于是 pre-v1.7.0 残留的 `开关.情感分析` 被合并进来 → 渲染出第三个框。
- 修复：`loadSettings` 改成**白名单取键**（`typeof x === 'boolean'` 校验，与同文件 `频率` 写法一致），
  幽灵框消失，下次保存时顺带把残留键从存储里清掉（向后兼容，只丢未知键）。
- 全仓库 grep「情感分析」**零命中**——字符串只可能来自用户的 `extensionSettings`。

### 2.2 v1.8.3 — 回路字段「物理相态与表征」+ 法术AI 提示词换新

- 新字段 `物理相态与表征`：`z.string().optional()`，代表**正文AI 叙事时的唯一形态锚点**。
- **改一个字段要同时动六处**（漏一处就断链）：`core/schema.ts`（zod，不声明会被 strip）、
  `pipeline/spell-ai.ts`（两个 json_schema + 结果类型 + 兜底）、**`assets/statusbar-desk.html`**
  （剧情获得 + 自创申报两个 json_schema、`getSpellPrompt` 兜底文案、两处入库写入、详情页 specs 的「形态」行）、
  **`assets/statusbar.html`**（旧六页版同样三处）、**`assets/opening.html`**（`BRANCH_CIRCUIT` 数据 +
  `circuitFor()` 写入）、`core/presets.ts` + `opening/grants.ts`（契约同步，二者不进 bundle）。
- ⚠️ **该字段绝不可写 `null`**：`z.string().optional()` 不收 null，会让**整个存档 parse 失败**；
  无值必须**省略键**（`p.表征` 缺失即 undefined，序列化时丢键）。已在 `AGENTS.md` §9.1 记下。
- ⚠️ 双桌 HTML 的 json_schema 带 `additionalProperties:false` → **不加进 schema 的字段 AI 根本输出不了**。
- 五系表征口径（写叙事时对齐）：倾泻=准直射束／波动·媒介波=波阵面+介质偏转+声学爆鸣（实体波与场=肉眼无形）／
  结构=**绝无弹道**+基底取材+晶格相态／生机=**绝无弹道光球**+渗透+组织异变／感知=无形多普勒球域+突触杂音。

### 2.3 v1.8.4 — 开局固定回路补「物理相态与表征」

- v1.8.3 只覆盖了**法术AI 的两条入库路径**，开局送的每条回路都缺该字段；偏偏 v1.8.3 又给详情页加了「形态」行，
  于是玩家一开局看到全是「—」。**这是必撞缺口，不是边缘情况。**
- 改法：`assets/opening.html` 的 `BRANCH_CIRCUIT`（**18 条**分支基础回路）各补 `表征` 字段，
  `circuitFor()` 返回值写入；`core/presets.ts`（4 条）+ `opening/grants.ts` 传参同步契约口径。
- 因开局 HTML 变更 → **重生成 `json/regex-开局.json`**。

### 2.4 v1.8.5 — 开局固定回路精神 ×4 门控 bug + 固化回路精神 ÷5 ★

用户反馈「开局送的固定回路精神消耗太高」。**两个独立缺陷叠加**：

1. **亲和门控键名错位（真 bug，影响全部回路）** —— 存档把亲和存成 `{族, 分支}`，而引擎
   `tierOf(aff, …)` 读的是 `aff.main[].fam` / `.br`。**三个 HTML 面板未做翻译**
   （`opening.html` 的 `ctxRef()`、`statusbar-desk.html` 的 `affinityOf()`、旧六页 `statusbar.html` 的
   `affinityOf()`）→ 亲和一律读空 → 全部落进「**全陌**」档 → 精神/能量统一 **×4**（本意 ×1）。
   `pipeline/settle.ts` 的 `affinityEngine()` **一直是对的**——只有面板漏了，且引擎**静默降级不报错**。
   - 修正：三处统一翻译成引擎契约 `{ fam: 中文族名, br: 分支 }`。
   - ⚠️ **只改「喂给 ctx」的那几处**；面板里另有**写存档**用的 `{族,分支}`（存档格式本身是对的）**故意不动**。
2. **固化回路无折扣** —— 世界书承诺固定回路精神 0~1，但引擎里只有 `findTuned` ×0.3 一条路径，
   多数固化回路落在微调预算外 → 实际无折扣。

**修复**：引擎新增 `TUNE.fixedMindMul = 0.2`；宿主在 `quote(req)` 里用 `req.固定 = (type==='fixed')`
声明固化回路，精神 ×0.2（=÷5）。**与 `findTuned` ×0.3 互斥**（改成 `if(req.固定)… else if(tuned)…`），防二次打折。
引擎号 2.2.0 → **2.3.0**。所有 `quote` 调用点补齐 `固定`：settle 的构建回路记录/⑥a 补挂/施放挂单、
`ops-table.confirmOps`、三个面板。

**实测**（默认 E 档开局，精神上限 20，主=媒介波，次=热/晶体）：
`风刃 11.1→0.6｜火球 26.9→2.7｜晶壁 11.8→1.2`；固定回路 0.6 vs 同参数自由回路 2.8（4.7×，符合设计）。
能量账单不受影响（只折算精神）。

### 2.5 v1.8.6 — 状态栏送审「送审中」可见状态 ★

用户反馈「送审时看不出有没有发出去，会反复点击」。查下去是**两个入口各有一个坑**：

1. **送审槽**（`reviewCircuit`）：`isReviewing = true` 之后**没有任何 UI 反馈**（按钮文案不变、槽位不变）；
   `btnReviewDock` 点击处理里 `if (isReviewing) return;` 又是**静默**返回 → 用户点了没反应只能再点。
   最隐蔽的一点：`updateReviewDock()` 里**早就有** `⟳ 审核中…` 分支，但**发起时根本没调用它**（只在完成后刷新才走到）。
2. **自由构筑「申报入自由库」**（`#btnSaveToRepo`）：**完全没有在途守卫** → 重复点击会并发多次 `generateRaw`，
   且每次 `.then` 都 `回路库.push(...)` → **重复入库**。比送审槽更糟：不只是没反馈，是真出错。

**修复（把「发起即反馈」当硬要求）**：
- 送审槽：`isReviewing` 置位后**立刻** `updateReviewDock()`；该方法扩展为同时切
  `#reviewDock.is-busy` / `#socket_review.is-busy` / 按钮文案 / **详情页 `btnReviewInDetail`** 忙碌态。
  槽内叠加 `review-busy-badge`（z-index 30，盖住卡牌 z-index 10）→ 旋转指示 +「送审中…请勿重复点击 /
  出结果会自动放回牌库」。遮罩顺带挡掉拖拽，一举两得。
- 自定义回路：新增 `renderForgeBusy(on)`（按钮禁用 + 改文案「⟳ 申报送审中…请勿重复点击」+ 下方
  `forgeBusyNote` 加载提示），并**补上原本缺失的令牌 + 20s 超时兜底**（原流程连超时都没有，AI 不返回就永久卡住）。
- 两入口共用 `spellReviewBusy()`（`isReviewing || isForging`）**互斥**，防并发扣费 / 双落地。
- 四条收尾路径（通过 / 驳回 / 超时 / 异常）统一复位，不会留下卡住的禁用按钮。

---

## 3. 验证状态（实测）

```
node build.mjs   → dist/index.js 335.4 KB(char) · 开局界面 80.1 · 状态栏-双桌 204.8 · 状态栏 73.0 · index.css 9.9
tsc --noEmit     → 0 错误（1 条 commonjs-variable-in-esm 无害警告，源自 UMD 引擎）
node smoke.mjs   → singleton { version: '1.8.6', status: 'ready' }，DOM 挂载/销毁完整，SMOKE OK
package-loader   → 23 项自检全 ✅，releases/酒馆助手脚本-开局.json 已更新
regex 包         → regex-开局 ≡ dist/开局界面.html（LF）· regex-状态栏 ≡ dist/状态栏面板-双桌.html（CRLF），均逐字节一致
```

> **数字口径提醒（别被体积吓到）**：`build.mjs` 打印的 KB 是 **`字符串.length / 1024`（字符数）**，
> **不是文件字节数**。中文在 UTF-8 占 3 字节，所以 `ls`/`statSync` 看到的 `dist/index.js` 约 **457 KB**，
> 而 build 打印 **335.4 KB**——两者都对，只是口径不同。写文档/对账时用 **build 打印值**（历史惯例）。

未做浏览器级人工验收（需酒馆宿主环境）；HTML 面板是独立脚本，只能做**内联脚本编译级**校验（见 §4）。

---

## 4. 可复用验证手法（本阶段沉淀，建议沿用）

1. **内联脚本编译校验**：把状态栏/开局 HTML 的 `<script>` 抽出来、用 `/*@ENGINE@*/` 注入真引擎后
   `new Function(code)` **只编译不执行** → 一行抓出编辑引入的语法错误；再配花括号/圆括号平衡做粗检。
2. **真引擎单测**：把 `circuit-engine.js` 复制成 `.cjs`（因 `package.json` 是 `type:module`，
   `require` 直读会拿到空对象）后 `require`，用**真存档**跑 `tierOf` 验证 main/mid/far 三档，
   对比修复前后报价差异。引擎静默降级、不抛错，**只能靠单测发现**（v1.8.5 的 ×4 bug 即此发现）。
3. **真 zod 实测**：`esbuild.build({ entryPoints:['src/gradband/core/schema.ts'], bundle:true,
   format:'cjs', write:false })` 就地编译成 CJS 加载，拿真 schema 跑用例（含新字段 PASS / 无字段旧档 PASS /
   `null` FAIL / 从 dist HTML 抽真实数据源逐条过 schema）。**脚本必须放在项目根目录跑**，否则 `import('esbuild')` 解析不到。
4. **regex 包重生成一律用「字符串级精确替换」**：读 raw → 取 `JSON.stringify(旧值)` 作字面量 →
   确认原文中**唯一命中** → 替换 → 校验前后缀**逐字节未变** + 换行风格（开局 LF / 状态栏 CRLF）。
   **别再依赖 parse→stringify 的格式假设**（历史上往返比对其实不通过，只是靠长度巧合没暴露）。
5. **产物抽查**：用 `node -e` 读 dist 里关键字附近的代码切片，确认线上真的换了逻辑——比只看源码硬。

---

## 5. 遗留 / 未处理（重要）

### 5.1 `docs/世界书条目-渐变带状态注入.txt` —— 用户手改，**未提交**
- 该文件在 **00:05** 被用户改动（晚于上次提交 23:56），与 `HEAD` / `docs/渐变带2.0 世界书.json` 的**条目 15**
  出现**分歧**：缺「锚点为「—」时…」「战斗中固定槽整排上锁」，但**多了**「必须点名回路」。
- v1.8.5、v1.8.6 两轮提交均**刻意排除它**（仅这一个文件停留在未暂存状态）。
- **待用户拍板**：把它同步回 JSON 条目 15（以用户版为准），还是保留 JSON 现状？
  ⚠️ 注意：JSON 只是**留档**，真正生效的是用户导进 SillyTavern 的世界书；两者本应保持一致（改一处必改另一处）。

### 5.2 数值平衡（非 bug，未改）
- E 档**爆发线仅 0.8kW**（100W×8），而火球/风刃抽取 ~9kW → 面板过载率显示 **1116% / 3139%**、`risk` 恒 100。
- 这是**爆发线功率太低**的数值问题，**与 v1.8.5 的门控 bug 无关**。已如实告知用户，留待单独调整
  （或考虑给固定回路加过载豁免）。**改它要动引擎 TUNE/爆发倍率，属数值改动，须走完整发版链。**

### 5.3 `ops-table.ts:3-5` 陈旧头注释
- 注释说「确认操作后挂待扣单 + 立即扣费」，但 `confirmOps()` 实际**只扣费不挂单**（真正挂单的是双桌 HTML 内联代码）。
- 连续几轮遗留未修（改 src 会触发发版链，故意攒着——**下次真正改代码时顺手修**）。

### 5.4 旧六页 `statusbar.html` 的同步程度
- v1.8.5 的 `affinityOf()` 键名修复 + `固定` 传参**已同步**到旧六页。
- v1.8.6 的「送审中」状态**只进了双桌版**（旧六页未同步；正则指向双桌版，旧六页不是主目标）。
  若要一并对齐，属新增功能，需单独评估工作量。

---

## 6. 接手注意（防回退清单，细节见 AGENTS.md）

### 6.1 两个「契约孤儿」——本项目最容易踩的架构坑
- **`pipeline/spell-ai.ts` 没有被任何模块 import**（`dist/index.js` 里搜 `spell_review`/`grant_review` = 0 命中），
  它已不是 bundle 的一部分，只是**文档化的契约**。
  **法术AI 真正的运行时全在状态栏 HTML 里**（`statusbar-desk.html` 两处：剧情获得送审 + 自创申报；
  `statusbar.html` 一处），各自自带 inline 提示词兜底 + 硬编码 json_schema。
  → **改法术AI 契约必须同步 HTML 里的 json_schema**，否则 `additionalProperties:false` 让模型根本输出不了新字段。
- **`opening/grants.ts` 的 `应用开局()` 全仓库零调用**（`dist/index.js` 里 `应用开局`/`预设回路清单`/
  `windcut` 均 0 命中），它与 `core/presets.ts` 一样是**没进 bundle 的契约孤儿**。
  → **开局建档唯一真实路径是 `assets/opening.html`**（正则注入的独立脚本，自己维护一份回路数据 `BRANCH_CIRCUIT`）。
  **改「开局送的东西」先确认改的是 HTML 那条活路径。**

### 6.2 引擎 ctx 键名契约（血的教训，v1.8.5）
- 引擎 `tierOf/gateOf` 吃的是 `{ fam: 中文族名, br: 分支 }`；存档 `亲和.主分支` 存的是 `{族, 分支}`。
  **任何喂 ctx 的出口都必须显式翻译**（`settle.ts` 的 `affinityEngine()` 是官方范式）。
  漏转会**静默**退化成「全陌」（精神×4、能量×4），同时污染报价、挂单扣除与基线账单三处。

### 6.3 操作习惯（踩过的坑，记牢）
- **同一文件的多处 Edit 不能并行发**（同消息内并行会互相覆盖）。**同文件多处修改必须串行**——
  v1.8.5 的 `circuit-engine.js` 折扣分支就被同批 Edit 覆盖过，靠 grep 复查才发现。
- **发版一律用轻量标签**：`git tag vX.Y.Z`（**不加 `-a`**），与 v1.8.1~v1.8.6 一致；打前先 `git tag` 查占用，
  打完可 `git cat-file -t vX.Y.Z` 复核应为 `commit`。
- **`quote(req)` 的 `固定` 位**：仅**固化（`type==='fixed'`）**回路可传 true；自由/现构回路**绝不能**传，
  否则白送 5 倍精神折扣。

### 6.4 铁律（依旧全部有效）
- 「**数字只被脚本改**」：AI/玩家不得自行算数，一切计算过引擎 E（`quote`/`settleCircuitLibrary`/…）。
- 业务代码**不许直接 import `circuit-engine.js`**，一律走 `engine/engine.ts`。
- 标准/特殊数据**靠存储位置隔离**：不要往 `__of_tables__` 塞引擎数据，也别让数据/法术AI 读标准表。
- 操作表确认**只扣费不挂单**（用户明确要求，别改回挂单）。
- 入口**只有悬浮球**（脚本按钮在主页面无效，别加输入栏按钮）。
- 陆安追踪表走**标准填表**（无独立感情AI）——别按旧文档把 feel-ai 加回来。
- 改任何 HTML 资产 → **必 build + 用户重导对应正则**；`dist/` **不能移动**（脚本版 CDN 靠它）。

---

## 7. 构建与发布（照 AGENTS.md §11–§12）

```bash
node build.mjs                                  # 产 dist/index.js + index.css + 三个 HTML
node node_modules/typescript/bin/tsc --noEmit   # 必须 0 错误
node smoke.mjs                                  # 冒烟：shim DOM 跑 init/destroy
node package-loader.mjs                         # 生成 releases/酒馆助手脚本-开局.json（23 项自检）
```

发版：①三处版本号同步（`core/version.ts` / `manifest.json` / `package.json`，当前 **1.8.6**）→
②build / tsc / smoke → ③`package-loader` 重生成 → ④**仅当 `dist/*.html` 有变更**才重生成 `json/regex-*.json`
（字符串级精确替换，别动其他字段）→ ⑤`git add -A && git commit && git push` +
`git tag vX.Y.Z && git push origin vX.Y.Z`（轻量标签；push 更扩展版、tag 更脚本版）。

---

## 8. 接手第一步建议

1. 读根目录 `AGENTS.md`（架构 + 规矩），再看本文件（现状 + 遗留）。
2. 需要引擎/HTML 契约细节时查 `docs/具体介绍.md`（按部分查，不必通读）。
3. `git log --oneline -10` 对齐版本；`git status --short` 确认只剩 §5.1 那个手改 txt。
4. 领任务前先想清「改的是 HTML 活路径还是 TS 契约孤儿」（§6.1）——**本项目一半的坑都在这**。
