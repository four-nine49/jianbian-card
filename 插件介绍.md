# 开局框架 · SillyTavern 插件简介

> 一个给 SillyTavern 用的**开局框架插件**：把"开局"做成可视化的表格数据 + AI 自动维护，
> 聊完开局就能直接玩，全程不用手动抄数值、不用碰代码。

---

## 这个插件解决什么问题

角色卡的开局通常要玩家手动填一堆设定：角色属性、技能、资产、地区、状态栏数据……
本插件把这些做成**结构化的表格**，并提供三种开局方式，开局数据自动写入、自动更新、
自动暴露给状态栏/前端界面使用。

## 核心功能

### 📜 三种开局方式
- **剑与汽水角色卡专用**：四条线（爱丽丝/沧月汐/似久/墨白）向导式开局——
  选角色 → 选性别 → 自动写入 9~10 张表的初始数据 → 预设开场白注入第 0 楼，
  全程点几下就完成。
- **AI 对话引导**：内置"自定义开局"规则包，AI 像主持人一样陪你聊出专属开局
  （模式/属性/金手指/同伴/开场白），聊完自动输出结算数据并落进表格。
  规则包支持导入/切换，换角色卡不用改代码。
- **手动建表**：从零定义任意表结构，自由玩法。

### 🤖 AI 自动填表
- 开局后**每 3 条 AI 回复自动更新一次表格**（角色状态、技能、资产、地区……AI 按你定的规则维护）；
- 独立的填表 API（可跟酒馆用同一个，也可单独配便宜快速的模型）；
- 表格分组：重要/复杂的表可以让一个 AI 单独填，不和别的表混；
- 支持 `json_schema` 强制结构化输出 + 流式请求（反代友好，可开关）。

### 📊 状态栏 / 前端界面数据源
- 自动把**所有表格快照**同步到楼层变量 `stat_data.开局框架`（收到回复、填表完成后自动同步，也可手动）；
- 自动在 AI 回复末尾追加 `<StatusPlaceHolderImpl/>` 锚点；
- 你的状态栏前端直接从楼层变量读数即可展示。

### 🖥 可视化面板（支持手机）
悬浮球点开即用，包含：
- **表格数据**：像 Excel 一样直接查看/修改所有数据
- **表结构/配置**：每张表的作用、维护规则、填表参数
- **填表参数 a 档**：所有表默认"跟随全局"，零配置直接玩；想细调的表单独切"自定义"
- **提示词模板**：填表提示词自由增删改，占位符带说明
- **设置 / 工具**：填表 API、全局默认、手动填表、数据同步、楼层清理，一页搞定

### 🌍 其它
- 填表自动带**世界书**（蓝灯全发、绿灯按关键词匹配最近对话）；
- 表格数据里的 `{{user}}` 等宏自动替换成实际名字；
- 正文提取标签（默认只取 `<content>…</content>` 之间的内容发给 AI）；
- 界面手机/窄屏自动适配（面板近全屏、侧栏横排）。

## 安装要求

- SillyTavern（较新版本）
- **酒馆助手（TavernHelper）扩展**，且先启用

## 两种安装方式

| 形态 | 方法 | 更新 |
|---|---|---|
| 扩展版 | 扩展管理器 → 安装扩展 → 填仓库地址 | 酒馆启动时自动拉取 |
| 酒馆助手脚本版 | 导入 `酒馆助手脚本-开局.json` 并启用 | 自动跟随最新 tag |

⚠️ 两种装一个就行，同时启用会互相拦截提示。

## 快速上手（三步）

1. 点右下角 **🎬 悬浮球**（或输入 `/opening`）打开面板
2. 剑与汽水卡用户：进「剑与汽水角色卡专用」→ 四步点完；
   其它卡用户：进「开局」→ 选「AI 对话引导」→ 跟着 AI 聊完
3. 开始玩。表格会自动更新，状态栏数据自动就位

## 适用范围说明

- 「剑与汽水角色卡专用」面板只适用于剑与汽水这张卡；
- 其它任何卡用「AI 对话引导」或「手动建表」，框架本身不挑卡。

---
*当前版本 v1.9.1 · 需要酒馆助手（TavernHelper）*
