# 渐变带 · 双桌工作台 会话交接文档（过载 / 手操 / 补给轮）

> **给接手的 AI**：这份文档记录「过载率/过载风险公式定稿 + 手操挂单扣费 + 一补给一牌」这一整轮会话对
> 「双桌工作台」（`src/gradband/assets/statusbar-desk.html`）及引擎/管线的全部改动、定稿决策、已修复的坑与待办。
> **先读这份**，再看旧的 `工作台交接文档.md`（billboard/zoom/锻造台架构轮）与 `渐变带交接文档.md`（架构总纲）、
> `状态栏HTML适配指南.md`（数据契约）。三份文档配合 `自由构筑-计算引擎-完整交付.md`/`渐变带-自由回路-引擎计算规格.md`
> （给外部 AI 的引擎交付包）即可完整接手。**本会话全部为双桌工作台轮，旧 statusbar.html（六页版）不是主目标。**

---

## 0. 当前状态一句话

- 仓库 `four-nine49/jianbian-card`，**git HEAD = d04cc93 = v1.5.0（已提交已推送）**；最新 tag = v1.5.0
- **工作区有一大批未提交改动**（见 `git status`）：`statusbar-desk.html`、`opening.html`、`statusbar.html`、
  `circuit-engine.js`、`engine.ts`、`settle.ts`、`schema.ts`、`presets.ts`、`grants.ts`、`data-ai.ts`、`contract.ts`、
  `scheduler.ts`、`serialize.ts`、`spell-ai.ts`、`core/settings.ts`、`ui/pages/gradband-data.ts`、`dist/*`、
  本文件、`渐变带交接文档.md`、`状态栏HTML适配指南.md`、`渐变带-自由回路-引擎计算规格.md`、
  `自由构筑-计算引擎-完整交付.md` —— **尚未提交、版本号未升（仍 1.5.0）**
- `git stash@{0}`：`core/settings.ts` + `feel-tables.ts` 的「感情AI每表提示词清理」被用户摘出暂存
  （**发布时不含**，用户明确不要；保留等用户决定恢复/丢弃）
- 依赖 esbuild 在 `../开局框架-扩展/node_modules`；tsc 也在那里。沙箱下 esbuild spawn 报 EPERM 时
  需 `danger-full-access` 重试（本轮 approval 已关，direct 运行可行）。

---

## 1. 本会话主题一句话

在手操/自由放置/补给体系上完成四条主线（全部**定稿落地**，勿回退）：

1. **手操 = 圈选挂待扣单，回合末统一扣**（不再当场扣费）
2. **点牌库的牌 → 自由放置到桌面**（不装槽；未送审牌因此能上桌看详情/送审）
3. **补给一补给一牌**（同名用 uid 编号区分）+ 详情页「回收（放回补给库）」按钮
4. **过载率 / 过载风险双指标公式定稿** + 引擎物理正确性修正（风刃/声波/引力波/磁场）

---

## 2. 数据流与关键文件地图

```
chat 变量「渐变带」= 六表游戏对象 g（主角/场景/回路库/固定库/补给物品/战斗旗…）
  ├─ src/gradband/engine/circuit-engine.js  ★全部数值引擎（UMD 挂 window.CircuitEngine，VERSION 2.1.0）
  │    含 settleCircuitLibrary 导出（结算时重算回路过载率/过载风险）; FAMS/SUBS 五族全参数
  ├─ src/gradband/engine/engine.ts           把 E.settleCircuitLibrary 等 bind 导出
  ├─ src/gradband/pipeline/settle.ts         ⑥a 补挂待扣单 / ⑥f 一补给一牌 / 使用补给 / ⑥j 重算过载
  ├─ src/gradband/core/schema.ts             回路/补给字段 schema（见 §5）
  ├─ src/gradband/core/presets.ts            预设补给 4 类 + 按纯度算效果 + 补给档位
  ├─ src/gradband/opening/grants.ts          开局预设补给展开为独立条（#开局k uid）
  ├─ src/gradband/assets/opening.html         同 grants 展开逻辑
  ├─ src/gradband/assets/statusbar-desk.html ★主体 UI（双桌工作台）
  ├─ src/gradband/assets/statusbar.html      旧版六页（手操 doOpsConfirm e:0 bug 仍存在，desk 是主目标）
  ├─ src/ui/pages/gradband-data.ts           数据页回路表（过载率/过载风险/状态 列）
  └─ src/gradband/pipeline/{data-ai,contract,scheduler,spell-ai}.ts
        data-ai: 剧情获得(待送审)/爆发线持续线变更/补给白名单纯度
        scheduler: 直接 settle（不自动送审）; spell-ai: 参数模板/钳制/送审函数（bundle 侧死代码）
产物：node build.mjs → dist/index.js + dist/状态栏面板.html + dist/状态栏面板-双桌.html（★交付物）+ dist/开局界面.html
      （引擎在 /*@ENGINE@*/ 处内联；无 iframe；正则注入：查找 <StatusPlaceHolderImpl/> 全文替换）
```

---

## 3. 定稿设计决策链（别回退！）

| 决策 | 落地方式 |
|---|---|
| **手操不当场扣费** | 手操 = 圈选槽位回路 → 挂**待扣单**（`manualOps` + `opsUsedSet` 去重=每轮一回路一次），回合末 settle **⑥a** 统一扣；数据AI 报的「本轮使用回路」中**未挂待扣单的补挂待扣单**（名称带「（补扣）」）——防免费施放；⑥b 只计次 `uses` |
| **点牌库 → 自由放置** | `renderRepoList` 点击直接写 `桌面位置 {x:1500+rand,y:1300+rand}`，任何审核状态都能上桌；**拖拽装槽**才检查送审（`可用()`）——未送审牌能上桌看详情/送审 |
| **一补给一牌** | settle ⑥f：报的数量 N → N 条独立记录（数量 1，uid=`名称-纯度#时间戳k`）；同名补给用 uid 编号区分（卡面 `#xxxx` 徽标）；补给详情有「回收（放回补给库）」按钮；旧数据无 uid 合并数量仍可 parse（×N 一张牌兜底） |
| **手操滑块文字** | 左「非手操」右「手操」（checked=手操）；`.ops-switch-box` |
| **物理正确优先** | 风刃能量 `½ρAv³×t×激波系数`（暴涨~120 倍，用户接受）；声波声强标准 `1e-12×10^(db/10)`（暴涨 1e4 倍，用户接受） |
| **过载双指标定稿** | 公式见 §4 |

---

## 4. 过载公式（定稿，勿改数值结构）

- `过载率 = power/基准线×100`（基准线 = relT≥1 ? 持续线 : 爆发线）
- `过载风险 = clamp(0,100, round(Φ_power + Φ_barrier + Φ_body))`
  - `Φ_power = f(R) × β × γ`
  - `Φ_barrier = (亲和罚 main0 / mid6 / far18 + (unf-1)×20) × β × Ω(P)`
  - `Φ_body = Σchip.w + sev×5 + (exhaust ? 20 : 0)`
- `f(R)`：R≤80→0；80-100→(R-80)×0.25；100-150→5+(R-100)×0.6；>150→35+(R-150)×1.0
- `β = (100/mMax)^0.5`；`γ = 0.8+(1-P)×1.2`；`Ω = 0.6+1.4×(1-P)²`；`P = clamp(mCur/mMax)`
- `power = (r.out||0.1)/relT`（真实做功功率，脱钩门控）
- 微调命中 `rawBarrier×0.3`；主修分支免大类陌生基底

落地：`circuit-engine.js`（settleCircuitLibrary 内）+ `settle.ts` ⑥j 用 `settleCircuitLibrary` 重算过载率+过载风险。

---

## 5. 引擎物理修正（circuit-engine.js）

| 回路 | 修正 | 效果 |
|---|---|---|
| 风刃 | `E=(0.5×1.2×A×v³×0.3/1000)×shockFactor`，`shockFactor=v>340?v/340:1` | 物理正确，暴涨~120 倍（用户接受）；desk 预设参数已调低（fa=20 fv=45 等）回到轻击区间 |
| 声波 | sknob=0 用 `I=1e-12×10^(db/10)` | 暴涨 1e4 倍（用户接受）；+194dB 失真 chip |
| 引力波 | `E=raw`（不保底 500）；`o.gravCost=500` 进 bill | 体内耗能不做功，锚点不再 TNT 级 |
| 磁场 | 新增 `mhold`（维持时长滑杆→relT）+ `mset`（建立时长→`o.chargeT`→readyT） | 基准线不再恒走持续线、功率不再被稀释 |
| 核力场 | 方案B 改文案「截面转化率0.1%」 | — |
| syncParams | flow `sub` 越界回落 | 防越界 |

**补给数值**：`presets.ts` —— 晶体×2 纯64.2 / 导液×2 纯52 / 止血喷雾×1 / 桥接贴片×1；按纯度算效果
（晶体 500/1800/4800 kJ、导液 12/30/50 点，档位 <80/80-99/≥99）；补给档位（战略/管控/民用）。

---

## 6. schema 字段（core/schema.ts，向后兼容）

- **回路**：`审核状态('免审'|'待送审'|'已通过')`、`过载率`、`过载风险`、`桌面位置`
- **补给**：`纯度`、`桌面位置`、`uid`（数量字段保留；新数据每张数量=1，uid 区分；旧数据无 uid 合并数量仍可 parse）
- **补给效果**：`目标('能量'|'精神'|'创伤')` + `身体状态`
- **补给白名单**：`['魔素晶体','魔素导液','快速生化止血喷雾','仿生神经桥接贴片']`
- 使用补给支持创伤（目标='创伤'，身体状态降级；顺序 正常<轻伤<重伤<过载透支）

---

## 7. 手操机制（statusbar-desk.html，勿回退实现细节）

- `manualOps` 布尔 + 滑块 `.ops-switch-box`（左「非手操」右「手操」）
- `opsUsedSet` 去重 → 每轮每个回路最多一次
- 槽位「使用」按钮 `.slot-use-btn`：**z-index:40 + pointer-events:auto**（防被卡牌 z-index:10 盖住）
- 启用条件：`manualOps && c`（圈选放行，确认时再拦送审）——**不要**用 `可用(c)`（旧数据审核状态 undefined 会不启用）
- `确认使用` → 挂待扣单：`makePending` 带 `名称: c.名称 + '（手操）'`，**ref = 回路 id**（契约校验 `pendingRefs.has(id)` 依赖它）
- 圈选后卡 + 槽灰化不发光：`.card-token.used` / `.slot-socket.used`
- **B1 待扣单字段强冲**（勿删）：`pend.名称 = pend.名; delete pend.名`（makePending 返回 `名` 非 `名称`，污染存档）

---

## 8. 槽位/卡片交互要点（geometry，勿回退）

- 卡 `card-token` 136×206，absolute 于 `surfaceDesk1`；槽位 `.slot-socket`（152×276）内 `.slot-well`
- 坐标系：`SOCKET_COORDS` + `updateSocketCoordinates` 用 **`getBoundingClientRect` + `/zoom1`** 居中
  —— ⚠️ **不要**换回 `offsetWithin`/offsetLeft 累积（不含 transform，`desk1-center` 有 translate(-50%,-50%)，
  会整体偏移导致拖拽吸附/装槽失效，已验证回退）
- 缩放走 `element.style.zoom`（重排非 transform），`zoom1` 参与坐标除法
- 补给卡 `supply-token`：`supplyKey(s) = s.uid || 名称|纯度`；`supplyBadge(s)` = uid 尾 4 位 `#xxxx`；
  `supplyNameHtml(item)` = 名称 + `<span class="sup-uid-n">#xxxx</span>`（补给品库列表/桌面卡名/详情页卡名三处接入）；
  `supplyScore` 展示条件：新 uid 卡数量=1 不显示 ×1，旧合并数据显示 ×N；详情横幅「单件 · 一牌一件 · #xxxx」
- 点补给卡 → 自由放置写 `桌面位置`；拖拽移动写回；`useSupply`/`recycleSupply` 均按 uid 精确操作
  （用完 数量≤0 删整条；回收 delete 桌面位置）

---

## 9. 已修复的坑（别让下一个 AI 重走弯路）

| 坑 | 根因 | 解法 |
|---|---|---|
| offsetWithin 吸附失效 | offsetLeft/offsetTop 不含 transform | `getBoundingClientRect + /zoom1` 版本 |
| fixed 卡回收消失 | `REPO_CARDS` 过滤 `type==='free'` | 改成 `!mounted.has(c.id)`（fixed+free 都进牌库） |
| 点「使用」没反应 | 卡牌 z-index:10 盖住按钮 + 启用条件含 `可用(c)` | `.slot-use-btn` z-index:40 + 条件 `manualOps && c` |
| 风刃能量暴涨 5.4→~638 kJ（~120 倍） | 物理公式 | 物理正确，用户接受；desk 预设参数调低 |
| 引力波 500kJ 保底矛盾 | 保底 500 与「实战≈0」冲突 | `E=raw`，起步 500 作 `o.gravCost` 进 bill |
| 声波声强 1e4 错位 | `1e4×10^((db-200)/10)` | 标准 `1e-12×10^(db/10)` |
| 磁场 mset 当 relT | mset(1-60s) 使基准线恒走持续线+稀释功率 | 新增 `mhold` 滑杆(0.1-60s) 作 relT，mset 进 readyT |
| B1 待扣单字段 bug | makePending 返回 `名` 非 `名称` | desk 强冲 `pend.名称=pend.名; delete pend.名` |

---

## 10. 验证命令（每次改完必跑）

```bash
cd src/渐变带-面板版
node build.mjs                                          # 产 dist（引擎内联入三个 HTML；esbuild 在 ../开局框架-扩展/node_modules）
node ../开局框架-扩展/node_modules/typescript/bin/tsc --noEmit -p tsconfig.json
node smoke.mjs                                          # 冒烟（SMOKE OK；stderr 的 TH 不可用/Slash 注册失败是 shim 正常现象）
node package-loader.mjs                                 # 改了 manifest 版本才跑
```

**引擎回归**（改了引擎才跑；⚠️ 该测试在**兄弟项目旧副本** `src/渐变带-自由回路/test/run.mjs`，
跑的是 渐变带-自由回路/src 的拷贝，**与面板版引擎 hashes 全 DIFF，覆盖力有限**）：46 用例全通过只代表旧副本状态。
面板版引擎真回归需把 settle-test 指向面板版 src 才有意义（未做，待办）。

**本轮验证状态**：build ✅ dist 已重新生成（状态栏面板-双桌.html 172.0 KB，grep 确认 supplyNameHtml/sup-uid-n/一牌一件 已内联）；
tsc ✅ 0 错误；smoke ✅ SMOKE OK；兄弟副本回归 46/46 ✅（仅供参考）。

---

## 11. 待办 / 注意

- **提交/发布未做**：全部工作区改动未提交、版本仍 1.5.0。用户确认效果后：`git add -A && git commit && push origin main`
  + `git tag v1.6.0 && push`（tag 先 `git tag --list` 查占用）；或升三处版本号（manifest/package.json/version.ts）走 1.5.1。
  **stash@{0} 里的 settings.ts/feel-tables.ts 用户明确不要，别 add 进去**（可 `git stash drop` 弃）。
- **每次改 HTML 资产后必须提醒用户重新导入正则**：`dist/状态栏面板-双桌.html` 查找 `<StatusPlaceHolderImpl/>` 全文替换
  （或重建工作台）；`dist/状态栏面板.html` 仍是流式旧版（并存）。
- **旧版 statusbar.html 手操 `doOpsConfirm` e:0 bug 未修**（desk 是主目标，低优先级）。
- **引擎回归测试未指向面板版**：`渐变带-自由回路/test/run.mjs` 测的是兄弟项目旧副本；要为面板版引擎建立真回归（待办）。
- 补给「一补给一牌」dawn 端：`useSupply`/`recycleSupply` 已按 uid 每张独立生效 ✅；渲染端 uid 编号徽标已接入 ✅。
- 桌面槽位目标：卡 136×206 贴合 slot-well（slot-socket 152×276）；坐标 getBoundingClientRect 居中（勿回退 offsetWithin）。
- 用户偏好：**物理正确优先**（接受大数值变化）；不喜欢自作聪明加 uses 计次（数据AI 会识别）；「点使用后变灰」
  指圈选后卡+槽都不发光（.used 已实现）。
- 待送审牌送审路径：点牌库→自由放置上桌→点桌面卡详情→「送审」按钮（desk 详情保留送审按钮）。
- 手操待扣单 ref = 回路 id（契约校验 `pendingRefs.has(id)` 依赖它）；名称带「（手操）」；补挂带「（补扣）」。
- 换对话框时的交接包 = 本文件 + 旧 `工作台交接文档.md` + `渐变带交接文档.md` + `状态栏HTML适配指南.md`
  + `自由构筑-计算引擎-完整交付.md`/`渐变带-自由回路-引擎计算规格.md`。

---

*本会话结束于当前轮；最后动作：补给「一补给一牌」渲染端收尾（uid 徽标三处接入 + 数量条件化 + 使用提示带编号），
build/tsc/smoke 全绿，dist/状态栏面板-双桌.html 已重生成；提醒用户重新导入正则。*