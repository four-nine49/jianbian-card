# 渐变带·自由回路 项目交接文档（给新 AI）

> 写给接手的 AI 编程助手。**先读这份**，再读 `渐变带-面板版/AGENTS.md`（开局框架底座通用文档）。
> 本文聚焦渐变带相关的一切：架构、数据流、两张 AI 体系的隔离、每个模块干什么、改哪里。
> 改完代码必须：`node build.mjs` → `tsc --noEmit` → `node smoke.mjs`。

---

## 0. 项目位置与形态

- **主目录**：`src/渐变带-面板版/`（发布仓库 `four-nine49/jianbian-card`）
- **形态**：渐变带·自由回路作为**开局框架扩展的一个面板**并入（路线 B）。开局框架底座（窗口/悬浮球/表格存储/填表/同步/规则包）全在，渐变带业务在 `src/gradband/`。
- **版本**：当前 1.4.3（manifest / package.json / src/core/version.ts 三处同步；改版必须三处一起改 + `node package-loader.mjs` 重生成脚本 json）
- **模式切换**：顶部「剑与汽水 / 渐变带」切换器（extensionSettings.appMode），侧栏按模式过滤（PAGES 的 modes 字段）

---

## 1. 核心架构：两套 AI 体系（最重要，别搞混）

| | **特殊 AI（数据 / 法术）** | **标准 AI（感情分析）** |
|---|---|---|
| 数据存储 | chat 变量 `渐变带`（六表引擎对象，zod 校验）| 开局框架表格 `__of_tables__`（type=standard 的表）|
| 提示词 | 「渐变带·提示词」页（gradband-prompts，特殊页）| 「提示词模板」页（**复用剑与汽水 prompt-template 页**，所有标准表共用一套）|
| 触发 | scheduler 回合管线（runDataAI / spell-ai）| scheduler 里 runFeelAI |
| 调用 | callAI（gradband/pipeline/ai-common.ts → 开局框架 callGenerate）| 同左 |
| 隔离 | 只读 chat 变量 `渐变带`，**看不到** `__of_tables__` 标准表 | 只读 `__of_tables__` standard 表，**看不到**六表引擎 |

**归属（type 字段）**：`Sheet/TableDef.type = 'standard' | 'special'`（默认 standard）。
- 标准表 = 感情表（陆安追踪表）+ 玩家自建表 → 标准 AI 处理、标准页（表格数据/表结构配置/提示词模板）可见
- 特殊表 = 六表引擎（在 chat 变量 `渐变带`，根本不在 `__of_tables__`）→ 特殊 AI 专用
- 隔离靠**存储位置**天然实现，不需要过滤逻辑；标准表 `enabled=false` 所以不参与标准自动填表调度

---

## 2. 目录地图（渐变带相关）

```
src/gradband/
├── index.ts              # 中枢：initGradband（启动调度）/ destroyGradband / 注入开局标记到最新楼
├── core/
│   ├── schema.ts         # 六表引擎 zod schema（主角/补给/回路库/槽位/亲和/待扣单/场景/感情追踪）
│   ├── store.ts          # loadGame/saveGame（chat 变量 `渐变带`）/ syncSnapshot（stat_data.渐变带 快照）
│   ├── settings.ts       # 渐变带设置（开关/频率/三路 API/提示词三套/感情提示词[表名]）；extensionSettings
│   ├── presets.ts        # 预设（回路清单/补给清单/新手包）
│   └── time.ts           # 剧情时间解析/格式化
├── pipeline/
│   ├── scheduler.ts      # ★ 回合管线：message_received → runTurn（数据AI→settle→感情AI→saveGame→sync→日志）
│   ├── data-ai.ts        # 数据AI：正文→变更包（json_schema）→契约校验（抽取JSON/校验变更包）
│   ├── spell-ai.ts       # 法术AI：自由描述送审（离线降级 localJudge）
│   ├── feel-ai.ts        # ★ 标准AI（感情）：遍历所有 standard 表，剑与汽水模板组装，写回第1行
│   ├── settle.ts         # ★ 回合结算（纯函数 a→h：扣费/计次/时间恢复/剧情变更/转正…）
│   ├── contract.ts       # 数据AI 输出的契约校验（抽取JSON / 校验变更包）
│   ├── serialize.ts      # 状态/场景/库索引 序列化（给 AI 看的中文形态）
│   └── ai-common.ts      # callAI（走开局框架 callGenerate）+ recentStory（按轮取正文）+ 组装提示词
├── engine/
│   ├── circuit-engine.js # ★ 自由回路核算引擎（UMD，挂 window/globalThis.CircuitEngine）
│   └── engine.ts         # UMD→具名导出（quote/initParams/localJudge/makePending…）
├── ops-table.ts          # ★ 操作表（chat 变量 `渐变带.操作表`）：状态栏手操 → 确认扣费（不挂待扣单）
├── feel-tables.ts        # 感情表注册（FEEL_TABLES：陆安追踪表 + numCols）+ 默认感情提示词（已改剑与汽水占位符）
├── opening/grants.ts     # 开局发放（新手包/自挑 → 应用开局）
└── assets/
    ├── statusbar.html    # ★ 状态栏面板（正则注入；含手操 tab；自带 writeGame 读写 chat 变量）
    └── opening.html      # 开局面板
```

---

## 3. 回合管线完整数据流（runTurn）

```
AI 回复 message_received（type∈normal/regenerate/continue/swipe）
  → scheduler 监听：busy 防重入 / shouldRun（频率.数据AI 取模）
  → runTurn：
      1. clearOps()                       清空操作表（每轮自动清空，状态栏手操不跨轮）
      2. loadGame()                       读 chat 变量 `渐变带`
      3. 数据AI（开关.自动结算）：runDataAI → 抽取JSON → 校验变更包
           → 通过：settle(g, pack)（⑥a 扣待扣单 / ⑥b 自由回路 uses+ / ⑥c Δt恢复 / ⑥e剧情变更…）
           → 契约拒绝：整包打回（report.error）
      4. 标准AI（开关.感情分析）：runFeelAI()
           → 遍历所有 standard 表（feel-ai.standardFeelTables）
           → 每表：读当前行 → 剑与汽水模板（promptTemplate）组装 → json_schema 按表头
           → 写回表格第 1 行（updateRow；数值列 0-17 硬约束）
      5. saveGame(g)                      六表引擎写回 chat 变量
      6. syncSnapshot(g)                  stat_data.渐变带 快照到最新楼
      7. 回合日志 → chat 变量 `渐变带日志`
  → finally：appendStatusMarker（开关.状态栏标记 → 楼末加 <StatusPlaceHolderImpl/>）
```

**手动入口**：管理窗口工具 tab「手动结算一次」= manualTurn() = 跑一遍 runTurn。

---

## 4. 操作表（手操系统）

- **存储**：chat 变量 `渐变带.操作表` = `{ manual, circuits[], counts[] }`
- **状态栏「手操」tab**：手操开关 + 回路勾选/次数（默认 1）→ 实时存 chat 变量 → 「确认操作」按钮
- **确认扣费（doOpsConfirm / confirmOps）**：quote 算费 → **只扣能量/精神（当场结清），不挂待扣单**（待扣单只留给 AI 报的回路，settle ⑥a 统一扣）→ 落盘 → 清空操作表
- **每轮自动清空**：runTurn 开头 clearOps()
- 存 chat 变量而非表格：状态栏 HTML 是正则注入的独立脚本，读不到扩展表格存储，只能读写酒馆变量

---

## 5. 状态栏 / 开局 HTML（正则注入）

- **不自动注册正则**（用户手动导入）：`dist/状态栏面板.html` + `dist/开局界面.html`（build emitHtml 从 src/gradband/assets 生成，内联引擎）
- **查找标记**：状态栏 `<StatusPlaceHolderImpl/>`、开局 `<渐变带开局/>`（工具 tab 有导入说明）
- **状态栏自带存档闭环**：`writeGame(mut)` → updateVariablesWith 改 chat 变量 `渐变带` + 300ms 后同步 stat_data 快照——玩家操作（施放/装槽/使用补给/应用场景/手操确认）都走它，和扩展 saveGame 同一存储
- **改状态栏 HTML 后**：必须 `node build.mjs` 重新生成 dist/状态栏面板.html，用户需**重新导入**正则

---

## 6. 感情表（标准 AI）现状

- `FEEL_TABLES`（feel-tables.ts）注册：陆安追踪表（headers：姓名/长期目标/短期目标/怎么看待主角/自洽/共情/解构/对主角的信任度；numCols：自洽/共情/解构/对主角的信任度；note=0-17 变化规则全量）
- 建表：ensureFeelTables（开局时自动建，type=standard，enabled=false）
- **提示词**：所有标准表**共用一套**（剑与汽水 promptTemplate，存 store/settings；用户改「提示词模板」页全局生效）；feel-ai 的 buildOrdered 用 `{{instructions}}/{{target_tables}}/{{table_data}}/{{messages}}/{{floor_info}}` 组装
- **写回**：updateRow 第 1 行；数值列 0-17 硬约束，坏值回退旧值
- 用户可在「表格数据」页删掉陆安追踪表——删了 standardFeelTables 就不再包含它（动态），标准 AI 跳过

---

## 7. 提示词页面分布（别混淆）

| 页 | 管什么 | 存哪 |
|---|---|---|
| 「渐变带·提示词」（gradband-prompts）| 特殊 AI：数据AI / 法术AI 两套（**没有感情AI**）| gradband settings.提示词 |
| 「提示词模板」（**剑与汽水 prompt-template 页**，渐变带模式复用）| 标准 AI：所有标准表共用一套 | store/settings.promptTemplate |

渐变带模式侧栏（纯文字，无 emoji）：
```
渐变带·自由回路 / 渐变带·提示词 / 渐变带·数据
表格数据 / 表结构/配置 / 提示词模板
设置 / 工具
```

---

## 8. 已知取舍 / 别乱改

- **标准/特殊隔离靠存储位置**：不要往 `__of_tables__` 塞六表引擎数据，也不要让数据/法术 AI 读标准表
- **操作表确认只扣费不挂单**：是用户明确要求（手操当场结清，待扣单留给 AI 报的回路）——别改回挂单
- **状态栏不自动注册正则**：用户手动导入；改 HTML 后要重新导入
- **感情 AI 用剑与汽水模板**：不是每表一套（v1.4.2 改过每表一套，v1.4.3 改回共用一套）——共用是当前正确状态
- **入口只有悬浮球**：脚本按钮主页面无效，别加
- **版本三处同步 + loader**：改版必做
- **module 问题**：smoke 里 `globalThis.module=undefined` 是故意的（让 UMD 引擎挂 window，浏览器默认如此）；engine.ts 读 CircuitEngine 兼收 window/globalThis

---

## 9. 快速验证

```bash
cd src/渐变带-面板版
node build.mjs                          # dist/index.js + 状态栏/开局 HTML
node ../开局框架-扩展/node_modules/typescript/bin/tsc --noEmit -p tsconfig.json
node smoke.mjs                          # 冒烟（DOM shim 跑 init/destroy）
node package-loader.mjs                 # 改版本后重生成 酒馆助手脚本-开局.json
```

**原渐变带引擎测试**：`src/渐变带-自由回路/test/run.mjs`（46 用例，业务逻辑没动过，改动引擎相关代码可跑它确认）。
